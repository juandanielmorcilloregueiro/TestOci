apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-ms-mocks.fullname" . }}
  labels:
    {{- include "chart-ms-mocks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-ms-mocks.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-ms-mocks.fullname" . }}-test-connection"
  labels:
    {{- include "chart-ms-mocks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-ms-mocks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
